% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliever_wrappers.R
\name{reliever_nmcd}
\alias{reliever_nmcd}
\title{Reliever with univariate nonparametric CDF loss}
\usage{
reliever_nmcd(
  data,
  cpn_max = 3,
  dm = 50,
  cov_rate = 0.8,
  method = "SN",
  cpn_crit = "none",
  pen_val = 1,
  prune_value = 0,
  M = 100,
  wbs_seed = NULL,
  wbs_stop_crit = NULL,
  detail = FALSE,
  cache_backend = "by_loss_block",
  owner_key = TRUE,
  echo = FALSE,
  dc_grid_size = NULL,
  cache_profile = NULL,
  run_cpd_ids = NULL,
  dc_grid = NULL,
  w_trunc = 0,
  sort_X = NULL
)
}
\arguments{
\item{data}{Numeric vector or one-column matrix.}

\item{cpn_max, dm, cov_rate, method, cpn_crit}{Common path controls; see
\code{\link{reliever}()}.}

\item{pen_val, prune_value, M, wbs_seed, wbs_stop_crit}{Common penalty and
search controls; see \code{\link{reliever}()}.}

\item{detail, cache_backend, owner_key, echo}{Common output and cache controls;
see \code{\link{reliever}()}.}

\item{dc_grid_size, dc_grid}{Common candidate-grid controls; see
\code{\link{reliever}()}.}

\item{cache_profile, run_cpd_ids}{Advanced reusable-cache and loss-output
controls; see \code{\link{reliever_generic}()}.}

\item{w_trunc}{Fraction removed from each tail of the ordered full-sample CDF
cutpoints. If the reference vector has length \eqn{m},
\code{floor(w_trunc * m)} cutpoints are omitted from both the lower and
upper tails. The default 0 keeps every cutpoint used by the criterion.}

\item{sort_X}{Optional sorted reference vector that defines the empirical-CDF
cutpoints. The default sorts the training observations in \code{data};
externally appended evaluation rows are excluded automatically. Model
wrappers may resolve this reference once and reuse it.}
}
\value{
A \code{reliever_result} object with the common structure documented
in \code{\link{reliever}()}.
}
\description{
Detect arbitrary changes in a univariate distribution by aggregating
empirical-CDF log losses. Unlike mean-change detection, this can respond to
changes in location, scale, or distributional shape.
The main built-in entry is \code{reliever(x, cpd_family = "nmcd")}; this
wrapper exposes all empirical-CDF arguments.
}
\section{Selection}{

Use \code{select_by_run(fit, cpn_crit = "sic")} to select K by minimizing
\eqn{CDF\ loss + \log(n)K}. A non-negative numeric criterion supplies
another additive penalty per changepoint.
}

\examples{
set.seed(2026)
n_seg <- 300
x <- c(
  rnorm(n_seg, mean = 0, sd = 0.2),
  rnorm(n_seg, mean = 4, sd = 0.2),
  rnorm(n_seg, mean = -4, sd = 0.2)
)
fit <- reliever_nmcd(
  data = x, cpn_max = 7, dm = 30, cov_rate = 0.8, method = "SN"
)
selected <- select_by_run(result = fit, cpn_crit = "sic")
selected
stopifnot(identical(selected$K_hat, 2L))
}
\references{
Zou, C., Yin, G., Feng, L., and Wang, Z. (2014).
Nonparametric maximum likelihood approach to multiple change-point
problems. \emph{The Annals of Statistics}, 42(3), 970--1002.
DOI: 10.1214/14-AOS1210.
}
\seealso{
\code{\link{reliever}()}, \code{\link{reg_fun_nmcd}()},
\code{\link{select_by_run}()}
}
