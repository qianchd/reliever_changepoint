% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliever_parametric.R
\name{reliever_em}
\alias{reliever_em}
\title{Reliever for exponential-family distribution changes}
\usage{
reliever_em(
  data,
  cpn_max = 3,
  dm = 50,
  cov_rate = 0.8,
  method = "SN",
  cpn_crit = "none",
  pen_val = 1,
  prune_value = 0,
  M = 100,
  wbs_seed = NULL,
  wbs_stop_crit = NULL,
  detail = FALSE,
  cache_backend = "by_loss_block",
  owner_key = TRUE,
  echo = FALSE,
  dc_grid_size = NULL,
  cache_profile = NULL,
  run_cpd_ids = NULL,
  dc_grid = NULL,
  family,
  size = NULL
)
}
\arguments{
\item{data}{Numeric vector or matrix with observations in rows.}

\item{cpn_max, dm, cov_rate, method, cpn_crit}{Common path controls; see
\code{\link{reliever}()}.}

\item{pen_val, prune_value, M, wbs_seed, wbs_stop_crit}{Common penalty and
search controls; see \code{\link{reliever}()}.}

\item{detail, cache_backend, owner_key, echo}{Common output and cache controls;
see \code{\link{reliever}()}.}

\item{dc_grid_size, dc_grid}{Common candidate-grid controls; see
\code{\link{reliever}()}.}

\item{cache_profile, run_cpd_ids}{Advanced reusable-cache and loss-output
controls; see \code{\link{reliever_generic}()}.}

\item{family}{Exponential-family distribution name.}

\item{size}{Number of trials, required for binomial and multinomial data.}
}
\value{
A \code{reliever_result} object.
}
\description{
Fit one distribution in each interval and use observation-level negative
twice log-likelihood as the segment loss. Supported families are
\code{"binom"}, \code{"multinom"}, \code{"pois"}, \code{"exp"},
\code{"geom"}, \code{"diri"}, \code{"gamma"}, \code{"beta"},
\code{"chisq"}, and \code{"invgauss"}.
}
\details{
The main built-in entry is \code{reliever()} with
\code{cpd_family = "em"}. Use \code{cv.reliever()} with the same inputs to select K by outer
sample-splitting.
}
\examples{
set.seed(2026)
x <- c(rexp(40, rate = 1), rexp(40, rate = 4), rexp(40, rate = 1))
fit <- reliever_em(
  data = x, family = "exp", cpn_max = 3, dm = 10,
  cov_rate = 0.6, method = "BS"
)
selected <- select_by_run(fit, cpn_crit = "sic")
selected
stopifnot(identical(selected$K_hat, 2L))
}
\seealso{
\code{\link{reg_fun_em}()}, \code{\link{cv.reliever}()}
}
