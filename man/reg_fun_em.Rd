% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reg_fun_parametric.R
\name{reg_fun_em}
\alias{reg_fun_em}
\title{Exponential-family interval loss}
\usage{
reg_fun_em(
  data,
  l,
  r,
  l_end = l,
  r_end = r,
  save_model = FALSE,
  is_virtual_run = FALSE,
  family,
  size = NULL
)
}
\arguments{
\item{data}{Numeric vector or matrix with observations in rows.}

\item{l, r}{Inclusive fitting-interval endpoints.}

\item{l_end, r_end}{Inclusive evaluation-interval endpoints.}

\item{save_model}{Retain fitted parameters in the returned \code{model}.}

\item{is_virtual_run}{Return loss-output metadata without fitting.}

\item{family}{Exponential-family distribution name.}

\item{size}{Number of trials, required for binomial and multinomial data.}
}
\value{
A list containing the observation-level \code{loss} matrix and,
when requested, the fitted \code{model}.
}
\description{
Estimate one distribution on rows \code{l:r} and return observation-level
negative twice log-likelihood on rows \code{l_end:r_end}. Supported family
names are grouped as follows:
\itemize{
\item \code{"binom"}, \code{"multinom"}, and \code{"pois"};
\item \code{"exp"}, \code{"geom"}, and \code{"diri"};
\item \code{"gamma"}, \code{"beta"}, \code{"chisq"}, and
\code{"invgauss"}.
}
Full distribution names are also accepted; see \code{family} below.
}
\details{
This is the low-level interval loss used by
\code{\link{reliever_em}()}; most users should call that wrapper.
}
\examples{
set.seed(2026)
x <- rexp(30, rate = 2)
out <- reg_fun_em(
  x, l = 1, r = 20, l_end = 21, r_end = 30,
  family = "exp", save_model = TRUE
)
head(out$loss)
out$model$param
}
\seealso{
\code{\link{reliever_em}()}, \code{\link{reg_fun_glm}()}
}
