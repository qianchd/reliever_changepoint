% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reg_fun_parametric.R
\name{reg_fun_lm}
\alias{reg_fun_lm}
\title{Linear-model interval loss}
\usage{
reg_fun_lm(
  data,
  l,
  r,
  l_end = l,
  r_end = r,
  save_model = FALSE,
  is_virtual_run = FALSE,
  intercept = TRUE
)
}
\arguments{
\item{data}{Numeric matrix with the response first and predictors afterward.}

\item{l, r}{Inclusive fitting-interval endpoints.}

\item{l_end, r_end}{Inclusive evaluation-interval endpoints.}

\item{save_model}{Retain fitted parameters in the returned \code{model}.}

\item{is_virtual_run}{Return loss-output metadata without fitting.}

\item{intercept}{Add an intercept to the interval model.}
}
\value{
A list containing the observation-level \code{loss} matrix and,
when requested, the fitted \code{model}.
}
\description{
Fit ordinary least squares on rows \code{l:r} and return squared residuals
for rows \code{l_end:r_end}. The first column of \code{data} is the response
and all remaining columns are predictors.
}
\details{
This is the low-level interval loss used by
\code{\link{reliever_lm}()}; most users should call that wrapper.
}
\examples{
set.seed(2026)
x <- matrix(rnorm(60), ncol = 2)
y <- 1 + x[, 1] - x[, 2] + rnorm(30, sd = 0.3)
out <- reg_fun_lm(
  cbind(y, x), l = 1, r = 20, l_end = 21, r_end = 30,
  save_model = TRUE
)
head(out$loss)
out$model$coefficients
}
\seealso{
\code{\link{reliever_lm}()}, \code{\link{reg_fun_glm}()}
}
